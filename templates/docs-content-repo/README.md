# __SITE_NAME__ Content

This is a pure documentation content repository.

It owns:
- `content/docs/**`
- `content/docs/**/meta.json`
- `topoodoc.content.json`

It does not own:
- Next.js app code
- Fumadocs shell code
- deployment code
- Cloudflare config

Those are owned by the `topoodoc` system repository.
